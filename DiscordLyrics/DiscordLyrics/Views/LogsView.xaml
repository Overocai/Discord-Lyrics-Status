<UserControl x:Class="DiscordLyrics.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DiscordLyrics.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance vm:LogsViewModel}" mc:Ignorable="d">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Logs" Style="{StaticResource Text.Display}" />
                <TextBlock Text="{Binding FileName}" Style="{StaticResource Text.Body}" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource Button.Ghost}" Width="110" Content="Refresh"
                        Command="{Binding RefreshCommand}" />
                <Button Style="{StaticResource Button.Ghost}" Width="130" Margin="10,0,0,0" Content="Open folder"
                        Command="{Binding OpenFolderCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="4">
            <TextBox Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True" BorderThickness="0"
                     Background="Transparent" Foreground="{StaticResource Brush.TextSecondary}"
                     FontFamily="Consolas, Cascadia Mono" FontSize="12"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap" Padding="14" />
        </Border>
    </Grid>
</UserControl>
